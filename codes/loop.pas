program WhileLoop;

var loopCount : Integer;

begin {WhileLoop} 
    loopCount := 1;
    while (loopCount < 11) do
    
        begin
        writeln ('Loop Number ', loop);
        writeln;
        loopCount := loopCount + 1;
        end;
end. {WhileLoop}